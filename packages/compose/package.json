{
  "name": "@lager-etiket/compose",
  "version": "2.0.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "3-stufige Kompositions-Pipeline: compute (pure) → renderSvg (pure) → raster (PNG via etiket/png)",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    }
  },
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "obuild",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint . && oxfmt --check .",
    "test": "vitest run"
  },
  "dependencies": {
    "@lager-etiket/lib": "workspace:*",
    "etiket": "^0.12.0"
  },
  "devDependencies": {
    "obuild": "^0.4.40",
    "typescript": "~5.9.2",
    "vitest": "^5.0.1"
  }
}
