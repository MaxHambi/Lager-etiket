/**
 * @lager-etiket/types — gemeinsam genutzte Typen.
 * Öffentliche API des Pakets.
 */
export type {
  BarcodeConfig,
  PlacementArea,
  PlacementConfig,
  OutputConfig,
  AppConfig,
} from "./config.ts";
export { DEFAULT_CONFIG } from "./config.ts";
